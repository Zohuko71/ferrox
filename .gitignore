# Rust build output (Cargo.lock is intentionally committed — this is a binary crate)
/target/

# Local config overrides with real API keys
config/local.yaml
config/local.*.yaml

# Environment files with secrets
.env
.env.local
.env.*.local

# Docker named volumes (created at runtime)
prometheus-data/
grafana-data/
lgtm-data/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo
